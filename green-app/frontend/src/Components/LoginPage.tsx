import Login from "./Login";

const LoginPage = () => {
  return (
    <div className="w-full flex flex-row justify-center">
      <title>Login</title>
      <Login />
    </div>
  );
};

export default LoginPage;
